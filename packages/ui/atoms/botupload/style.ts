import type { SxProps } from '@mui/material';

interface BotUploadStyleProps {
  [key: string]: SxProps;
}

export const botUploadStyle: BotUploadStyleProps = {
  rootSx: {
    position: 'relative',
    cursor: 'pointer',
  },
};
